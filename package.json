{
  "name": "edutrak-monorepo",
  "private": true,
  "scripts": {
    "type-check:server": "npm run type-check --workspace=server",
    "format:check": "npm run format:check --workspace=server",
    "lint": "npm run lint --workspace=server",
    "db:generate": "npm run db:generate --workspace=server",
    "db:validate": "npm run db:validate --workspace=server",
    "db:migrate:deploy": "npm run db:migrate:deploy --workspace=server",
    "db:seed": "npm run db:seed --workspace=server",
    "test:unit": "npm run test:unit --workspace=server",
    "test:integration": "npm run test:integration --workspace=server",
    "build": "npm run build --workspace=server",
    "security:audit": "npm run security:audit --workspace=server"
  },
  "workspaces": [
    "server",
    "frontend"
  ]
}
