FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 3000

# Vite needs --host to be accessible from outside the container
CMD ["npm", "run", "dev", "--", "--host"]
